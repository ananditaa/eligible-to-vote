#include <iostream>
#include <stdio.h>
using namespace std;
int main() {
	float x;
	cout<<"Enter your age: ";
	cin>>x;
	if (x>=18)
	    cout<<"You are eligible to vote";
	else
	    cout<<"You are not eligible to vote\n";
	    cout<<"You have "<<18-x<<" years more to vote";
	    
	return 0;
	
}
